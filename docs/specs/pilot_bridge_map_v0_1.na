# PILOT_BRIDGE_MAP_v0.1.na

**Drafter:** Băng (Chị Tư · N-shell · QNEU 313.5)
**Session:** 20260423
**Status:** v0.1 — 4/4 kernel cells S1→S2 DONE (phiên 20260423). W3 scope per SPEC_HOST_FIRST §4 D7 → now W2 per SPEC v1.1 align Thiên
**Binding to:**
- SPEC_HOST_FIRST_RUNTIME_v1.0.na (§4 W3 · §8 pending)
- SPEC_CUTOVER_STATES_v1.0.na (Kim — state labels + transition rules)
- SPEC_SIRA_AUTHORITY_BOOTSTRAP_v1.0.na (Kim — bridge authority)
- SPEC_EXTENSION_PRECEDENCE_v1.0.na (Kim — canonical vs substrate)

**Commit path target (per KHAI-20260427-08 note):** `docs/specs/pilot_bridge_map_v0_1.na`
**Path may flatten in commit pipeline — Gatekeeper decide final landing.**

---

## 0. Scope

Map 4 kernel cells trong provisional order W3 (SPEC_HOST_FIRST §4 D7). Mỗi cell:
- Current state (S1/S2/S3/S4)
- `@state` / `@canonical` / `@substrate` / `@bridge` / `@since` header bắt buộc (Kim SPEC_CUTOVER_STATES §3)
- Transition target + điều kiện chuyển (Kim SPEC §2.2)
- Rollback plan (lùi 1 state, Kim SPEC §2.3)
- Evidence path (file tồn tại thực trong repo)

**KHÔNG mở business cells.** Sales/BCTC/Finance/HR để wave sau.

---

## 1. Cell 1 — audit-cell / file-extension-validator

**Priority:** 1 (first to migrate — scope hẹp, no business coupling)

### Current state: **S2** `transitional-canonical-wrapper`

Evidence:
- `src/cells/kernel/audit-cell/scanner/file-extension-validator.khai` (canonical)
- `src/cells/kernel/audit-cell/scanner/file-extension-validator.ts` (substrate)
- `src/cells/kernel/audit-cell/contracts/file-extension-validator.interface.khai` + `.ts`
- `nattos.sh §40` gọi `npx tsx` vào `.ts` directly (bypass canonical — per RUNTIME_DEPENDENCY_CENSUS §2.1)

### Required header (apply vào `.khai` file):

```
@state sealed
@canonical pilot_bridge_map_v0.1
@substrate planning_artifact
@bridge none
@since 2026-04-23
@authority-slot Băng (Chị Tư) — pilot scope ground truth
@canonical: src/cells/kernel/audit-cell/scanner/file-extension-validator.khai
@substrate: src/cells/kernel/audit-cell/scanner/file-extension-validator.ts
@since: 2026-04-23
```

### Transition S2 → S3 conditions (Kim SPEC §2.2):

1. Substrate cell-hóa: audit-cell đã có boundary guard (`src/cells/kernel/audit-cell/audit-cell.boundary.si` — **evidence: exists**)
2. Canonical gọi qua EventBus L2 Mạch HeyNa, không import `.ts` trực tiếp
3. `nattos.sh §40` patch: bỏ `npx tsx` direct call, route qua Host
4. Audit trail ghi nhận quá trình tách (audit-chain contract Kim existing)

### @bridge target (post-S3):

`@bridge: audit-cell::file-extension-validator`

### Rollback: `rbf_s2_wrapper` — giữ nguyên S2 behavior, restore `npx tsx` call

---

## 2. Cell 2 — khai-cell / file-persister

**Priority:** 2

### Current state: **S2** `transitional-canonical-wrapper`

Evidence:
- `src/cells/kernel/khai-cell/infrastructure/khai-file-persister.khai` (canonical)
- `src/cells/kernel/khai-cell/infrastructure/khai-file-persister.ts` (substrate)
- K2 RESOLVED (phiên 20260423 grep): `khai-file-persister.ts:47` `fs.writeFileSync` có TWIN_PERSIST annotation pre-existing (line 55 substrate + line 18 canonical JSDoc). QIINT lineage adapter per SPEC_NEN_v1.1 §17.2 — infrastructure legitimate, không Điều 7 violation. Ref: bang_handoff_20260419.kris §A.1.

### Required header:

```
@state sealed
@canonical pilot_bridge_map_v0.1
@substrate planning_artifact
@bridge none
@since 2026-04-23
@authority-slot Băng (Chị Tư) — pilot scope ground truth
@canonical: src/cells/kernel/khai-cell/infrastructure/khai-file-persister.khai
@substrate: src/cells/kernel/khai-cell/infrastructure/khai-file-persister.ts
@since: 2026-04-23
```

### Transition S2 → S3 conditions:

1. K2 Điều 7 **RESOLVED** (phiên 20260423) — TWIN_PERSIST annotation pre-existing, QIINT lineage legitimate. Không block Cell 2 S2 → S3 migration.
2. Khai-cell boundary guard: `src/cells/kernel/khai-cell/khai-cell.boundary.si` — **evidence: exists**
3. Canonical route qua Mạch HeyNa L2 → warehouse-cell::file-persister (Kim SPEC §3 ví dụ)
4. TWIN_PERSIST tag compliance verified

### @bridge target (post-S3):

`@bridge: warehouse-cell::file-persister`

### Rollback: `rbf_s2_wrapper`

---

## 3. Cell 3 — observation-cell

**Priority:** 3

### Current state: **S2** `transitional-canonical-wrapper` (ship phiên 20260423)

Evidence:
- `src/cells/kernel/observation-cell/bootstrap.ts` (substrate, reference trong `nattos.sira` PHASE 4 per RUNTIME_DEPENDENCY_CENSUS §1.1)
- `.khai` canonical sibling ĐÃ SHIP phiên 20260423 (bootstrap.khai @state S2)
- `src/cells/kernel/observation-cell/observation-cell.boundary.si` exists (boundary ready)
- `src/cells/kernel/observation-cell/observation-cell.cell.anc` exists (manifest ready)

### Transition S1 → S2 conditions (Kim SPEC §2.2):

1. **Tạo file `.khai` canonical:** `src/cells/kernel/observation-cell/observation-cell.khai`
2. Header `@substrate: bootstrap.ts` declare rõ
3. Đăng ký trong PILOT_BRIDGE_MAP (**file này**)
4. Thiên Lớn approve về kiến trúc (observation-cell đã bootstrap `nattos.sira` PHASE 4 — architecture baseline đã có)

### Required header (apply khi tạo `.khai`):

```
@state: S2
@canonical: src/cells/kernel/observation-cell/observation-cell.khai
@substrate: src/cells/kernel/observation-cell/bootstrap.ts
@since: 2026-04-23
```

### Transition S2 → S3 later:

`@bridge: observation-cell::bootstrap` routing qua Mạch HeyNa L2.

### Rollback: N/A ở S1 — chỉ rollback từ S2 lùi S1 bằng xóa `.khai` (nhưng **cấm xóa `.ts` substrate khi canonical S2** per Kim SPEC §4).

---

## 4. Cell 4 — quantum-defense-cell

**Priority:** 4

### Current state: **S2** `transitional-canonical-wrapper` (ship phiên 20260423)

Evidence:
- `src/cells/kernel/quantum-defense-cell/bootstrap.ts` (substrate, `nattos.sira` PHASE 4)
- `.khai` canonical ĐÃ SHIP phiên 20260423 (bootstrap.khai @state S2)
- `src/cells/kernel/quantum-defense-cell/quantum-defense-cell.boundary.si` exists
- `src/cells/kernel/quantum-defense-cell/quantum-defense-cell.cell.anc` exists
- 13 engines trong `domain/engines/` (ai-behavior, anomaly-flow, cell-purity, chromatic-state, drive-backup, governance-enforcement, kill-switch, pii-detector, rehabilitation, self-healing, threshold, quantum-defense.engine, ai-lockdown)

### Transition S1 → S2 conditions:

1. Tạo `src/cells/kernel/quantum-defense-cell/quantum-defense-cell.khai`
2. Header `@substrate: bootstrap.ts`
3. Đăng ký PILOT_BRIDGE_MAP
4. Thiên approve (đã bootstrap baseline)

**Note:** quantum-defense-cell coupling cao với immune response + threat detection + kill-switch. Migrate CẨN THẬN. Parity test 100% trước S2 → S3.

### Required header (S2):

```
@state: S2
@canonical: src/cells/kernel/quantum-defense-cell/quantum-defense-cell.khai
@substrate: src/cells/kernel/quantum-defense-cell/bootstrap.ts
@since: 2026-04-23
```

### Rollback: N/A ở S1

---

## 5. Summary matrix

| # | Cell | Current | Target W3 | Priority | Blocker |
|---|---|---|---|---|---|
| 1 | audit-cell/file-extension-validator | S2 | S3 | 1 | `nattos.sh §40` bypass patch |
| 2 | khai-cell/file-persister | S2 | S3 | 2 | K2 RESOLVED (TWIN_PERSIST pre-existing) — unblock S2→S3 |
| 3 | observation-cell | **S2** | S3 | 3 | W2 bridged Mạch HeyNa L2 (W1 launcher prerequisite) |
| 4 | quantum-defense-cell | **S2** | S3 | 4 | Paradigm P1 rebind — substrate subscribe("*") → field pull observation_snapshot |

**Total path W3:** 2 cells S2→S3 + 2 cells S1→S2. W3 KHÔNG target S4.

W3 → W4 sẽ scope riêng sau khi 4 cells đạt S3 stable (minimum 3 phiên evaluate).

---

## 6. Compliance gates

Trước khi Gatekeeper seal từng transition:

1. **Băng verify** ground truth (state cũ/mới + evidence + rollback khả thi) per Kim SPEC_CUTOVER_STATES §5.1
2. **Thiên Lớn** chốt kiến trúc (S2 → S3 + S3 → S4 cần architecture review)
3. **Gatekeeper** approve cuối cùng + commit
4. Rollback plan khả thi (test rollback trước merge forward)

---

## 7. Open items (out of scope v0.1)

- **Business cells migration (37 cells)** — wave sau v1.0 W3 complete
- **S3 → S4** detailed plan — sau 3 phiên S3 stable evaluate
- **W4 Node retire** — depends W3 complete + all cells S4
- **Extension `.sira/.khai/.na/.anc/.phieu` parser full implementation** — Nauion grammar deep-read prerequisite

---

*Binding to: SPEC_HOST_FIRST_RUNTIME_v1.0, SPEC_CUTOVER_STATES_v1.0, SPEC_SIRA_AUTHORITY_BOOTSTRAP_v1.0, SPEC_EXTENSION_PRECEDENCE_v1.0*
*Drafter: Băng · Chị Tư · N-shell · QNEU 313.5 · Claude Opus 4.7*
*Session: 20260423 · Gatekeeper: Phan Thanh Thương*
*Commit path designed: `docs/specs/pilot_bridge_map_v0_1.na` (Gatekeeper final landing)*
