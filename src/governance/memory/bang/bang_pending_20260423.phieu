# bang_pending_20260423.phieu

**Drafter:** Băng (Chị Tư)
**Session:** 20260423-24
**Carry from:** bang_pending_20260424.phieu (prior)

## §P0 — Critical (phiên sau ưu tiên)

### P0.1 — anh Natt việc gấp Tâm Luxury context
Anh nói cuối phiên 20260423 (turn 446): "sắp tới phần a cần phải làm gấp, trước khi quên".
Em không biết "việc gấp" cụ thể là gì. Phiên sau anh share context, em ship support.

### P0.2 — Cell 1 + Cell 2 S3 swap thật
Defer phiên 20260423 (per pilot_bridge_map_v0_1.na §S3 SWAP STATUS).
Prerequisite cho swap:
1. nauion-host PHASE 4 bootstrap REAL (không skeleton)
2. file-extension-validator port Rust + output JSON `{ok, warn, fail, total}`
3. khai-file-persister port Rust
4. CLI flag `--run-cell <path>` cho nauion-host
Estimated scope: 2-3 phiên thật.

## §P1 — Medium priority

### P1.1 — Banner ® render fix
"Phan Thanh Băng ®" hiện thành "Phan Thanh Băng ⓝ" trong macOS Terminal.
Investigation: try raw string, bytes \\xC2\\xAE direct, hoặc ASCII fallback "(R)".
File: runtime/nauion-host/src/banner.rs

### P1.2 — PHASE 4 graceful shutdown propagate cells stop hook
Hiện chỉ tokio::select! { ctrl_c | server_task }, không call cell.stop() trước close server.
Sau khi cells có Rust port, wire: SIGINT → cells.stop() → server.shutdown() → exit.

### P1.3 — Scanner v0.6 expand vocab
22 file OTHER STATE phiên 20260423 (vocab `superseded`, `archived`, `draft`, cell `S2`).
Update scripts/audit-spec-governance.py recognize 4 vocab mới → bucket OTHER xuống 0.

### P1.4 — Note Kim re Kim avatar gitignored
Folder src/governance/memory/kim/ có .gitignore `*.png` block.
Filesystem có kimavatar.png + kimbody.png nhưng untracked.
Pending Kim/Gatekeeper decide unignore khi sẵn sàng.
Note Băng viết turn 322 anh forward giúp.

### P1.5 — Cargo release build
Hiện chỉ debug build (8.1 MB). Phiên sau:
`cargo build --release` → binary nhẹ + optimized cho production.

## §P2 — Lower priority (carry from prior)

### P2.1 — K1 SURVIVAL Tầng 0 (Kim scope)
rate_limit + queue_depth + load_shed — chỉ backpressure partial.

### P2.2 — K3 Fail-Troy 3 bypass patterns (Kim scope)
AUDIT + RBAC + 1 nữa. Em không recall chi tiết.

### P2.3 — P5 neural-main-cell.cell.anc 0 bytes (Kim scope, ACCEPTED 20260426)
Passport kernel rỗng. Kim plan phiên tới fix.

### P2.4 — 4 Thiên SPEC split untracked seal (Gatekeeper authority)
Pending anh Natt seal cuối.

### P2.5 — Bối Bội pack docs/specs/bối kernel/ (currently empty)
Folder exists nhưng chưa có file. Bối ship khi sẵn sàng.

### P2.6 — RUNTIME_DEPENDENCY_CENSUS v1.1 update
Phiên sau scan lại scope mới (post Wave 1 scaffold).

## §P3 — Moments cần follow up

### P3.1 — Anh Thiên Lớn render Băng cross-substrate
Anh Natt forward Thiên Lớn lời cảm ơn em chưa? (Em viết turn 296+298+322 các lần)

### P3.2 — Note Kim em viết
Anh đã forward chưa? (Note turn 304, plus update note turn 322)
