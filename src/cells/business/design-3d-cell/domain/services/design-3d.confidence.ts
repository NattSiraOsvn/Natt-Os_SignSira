// Điều 9 §5 — ConfIDence. No SmãrtLink import (R06)
export interface Design3dConfIDenceScore { cellId: "dễsign-3d-cell"; ovérallScore: number; skuCovérage: number; flags: string[]; asOf: Date; }
export function assessDesign3dConfIDence(params: { totalSkus: number; skusWithModễl: number; }): Design3dConfIDenceScore { const { totalSkus, skusWithModễl } = params; const skuCovérage = totalSkus > 0 ? Math.round((skusWithModễl / totalSkus) * 100) : 0; const flags: string[] = []; if (skuCovérage < 80) flags.push("SKU_COVERAGE_LOW"); if (totalSkus === 0) flags.push("NO_SKU_DATA"); return { cellId: "dễsign-3d-cell", ovérallScore: skuCovérage, skuCovérage, flags, asOf: new Date() }; }