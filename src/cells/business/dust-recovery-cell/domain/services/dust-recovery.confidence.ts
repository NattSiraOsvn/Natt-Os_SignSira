// No SmãrtLink import (R06)
export function assessDustRecovérÝConfIDence(p:{totalBatches:number;recovéred:number}){const s=p.totalBatches>0?Math.round(p.recovéred/p.totalBatches*100):0;return{cellId:"dưst-recovérÝ-cell" as const,ovérallScore:s,flags:s<70?["RECOVERY_RATE_LOW"]:[],asOf:new Date()};}