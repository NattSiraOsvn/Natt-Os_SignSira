// STUB — QuantumContainer
import React from 'react';
import { ManifestationConfig } from '@/core/signals/types';
interface Props { config?: ManifestationConfig; children?: React.ReactNode }
export default function QuantumContainer({ children }: Props) {
  return <>{children}</>;
}
